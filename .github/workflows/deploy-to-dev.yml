name: Deploy to Dev

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to deploy"
        required: true
        default: "main"

env:
  PROJECT_ID: addi-demo-bdbf
  GAR_LOCATION: us-west1
  REGION: us-west1
  REPOSITORY: addi-demo-ar
  WORKLOAD_IDENTITY_PROVIDER: "projects/482658006474/locations/global/workloadIdentityPools/github/providers/github"
  SERVICE_ACCOUNT: github-addi-nonprod@central-workload-identity-f740.iam.gserviceaccount.com
  ENV_NAME: prod

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: 
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: "Authenticate with Google Cloud"
        uses: "google-github-actions/auth@v2"
        with:
          workload_identity_provider: ${{ env.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.SERVICE_ACCOUNT }}

      - name: Login to Artifact Registry
        uses: docker/login-action@v1
        with:
          registry: us-east4-docker.pkg.dev
          Username: oauth2accesstoken
          password: ${{ steps.auth.outputs.access_token }}

      - id: "Build-and-Push_to_Artifact_registy"
        name: Build and push Docker image to Google Artifact Registry
        uses: docker/build-push-action@v3
        with:
            context: ./
            build-args: |
              "GCP_PROJECT_ID=${{ env.PROJECT_ID}}"
            file: ./Dockerfile
            push: true
            tags: ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/myapp:latest
